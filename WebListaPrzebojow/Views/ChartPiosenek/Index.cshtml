@model IEnumerable<DAL.ChartPiosenek>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.chart)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.chart.ChartID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChartPiosenekID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChartPiosenekID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChartPiosenekID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
